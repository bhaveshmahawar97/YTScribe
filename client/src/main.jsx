import { createRoot } from 'react-dom/client';
import App from './App.jsx';
import './index.css';

// This is the main entry point for the React app (JavaScript version)
createRoot(document.getElementById('root')).render(<App />);
